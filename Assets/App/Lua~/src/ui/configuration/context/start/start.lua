return {
    mvp = {
        model = require("ui.start.StartModel"),
        view = require("ui.start.StarView"),
        presenter = require("ui.start.StartPresenter")
    },
    type = 'single',
    prefab = "Assets/AppData/Prefab/STS/View/StartView.prefab",
}