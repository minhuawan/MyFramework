return {
    mvp = {
        model = require("ui.main.MainModel"),
        view = require("ui.main.MainView"),
        presenter = require("ui.main.MainPresenter")
    },
    type = 'switchable',
    prefab = "",
}